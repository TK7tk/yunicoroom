# AI動向レポート自動生成システム

## 📋 概要

このシステムは、24時間以内の米国AI動向を自動的に収集・分析し、レポートを生成してGitHubにプッシュ、LINEで配信する仕組みです。

## 🗂️ ファイル構造

```
yunicoroom/
├── articles/
│   ├── 2025-12-31_ai-trend-report.md
│   ├── 2026-01-01_ai-trend-report.md
│   └── ...
├── scripts/
│   ├── daily_ai_report.sh       # メイン実行スクリプト
│   ├── send_to_line.py          # LINE配信スクリプト
│   └── README.md                # このファイル
└── .github/
    └── workflows/
        └── daily-ai-report.yml  # GitHub Actionsワークフロー
```

## 🚀 使用方法

### 1. 手動実行（ローカル）

```bash
cd /path/to/yunicoroom
./scripts/daily_ai_report.sh
```

### 2. GitHub Actionsによる自動実行

- **自動実行**: 毎日日本時間 朝9時（UTC 0:00）に自動実行
- **手動実行**: GitHubリポジトリの「Actions」タブから手動トリガー可能

#### 手動実行手順

1. GitHubリポジトリにアクセス: https://github.com/TK7tk/yunicoroom
2. 「Actions」タブをクリック
3. 左サイドバーから「Daily AI Trend Report」を選択
4. 「Run workflow」ボタンをクリック
5. （オプション）カスタム日付を入力
6. 「Run workflow」を実行

## ⚙️ 設定

### 環境変数

以下の環境変数が必要です（GitHub Actionsの場合はSecretsに設定）：

- `GITHUB_TOKEN`: GitHubへのプッシュ用（自動設定）
- `LINE_CHANNEL_ACCESS_TOKEN`: LINE配信用（オプション）

### Git設定

スクリプトは以下のGit設定を自動的に行います：

```bash
git config user.name "Manus AI Bot"
git config user.email "ai-bot@manus.im"
```

## 📝 レポート形式

### ファイル名

`YYYY-MM-DD_ai-trend-report.md`

例: `2025-12-31_ai-trend-report.md`

### 内容構成

1. **AI基盤技術ニュース**: 最新モデル、アップデート情報
2. **新興企業・スタートアップ動向**: 資金調達、ローンチ情報
3. **戦略的インサイト**: 市場分析と将来展望
4. **参考文献**: 情報源のリスト

## 🔧 カスタマイズ

### 実行時間の変更

`.github/workflows/daily-ai-report.yml`のcron設定を編集：

```yaml
schedule:
  - cron: '0 0 * * *'  # UTC 0:00 = JST 9:00
```

例：
- 毎日12時（JST）: `cron: '0 3 * * *'`
- 毎日18時（JST）: `cron: '0 9 * * *'`

### レポート内容のカスタマイズ

`daily_ai_report.sh`のテンプレート部分を編集：

```bash
cat > "${WORK_DIR}/report_template.md" << 'EOF'
# ここにカスタムテンプレートを記述
EOF
```

## 🐛 トラブルシューティング

### レポートが生成されない

1. スクリプトの実行権限を確認:
   ```bash
   chmod +x scripts/daily_ai_report.sh scripts/send_to_line.py
   ```

2. ログを確認:
   ```bash
   ./scripts/daily_ai_report.sh 2>&1 | tee debug.log
   ```

### GitHubプッシュが失敗する

1. Git認証情報を確認
2. リポジトリの書き込み権限を確認
3. ブランチ保護ルールを確認

### LINE配信が失敗する

1. MCP設定を確認:
   ```bash
   manus-mcp-cli tool list --server line
   ```

2. 認証トークンを確認

## 📚 参考資料

- [GitHub Actions ドキュメント](https://docs.github.com/ja/actions)
- [Cron 式リファレンス](https://crontab.guru/)
- [LINE Messaging API](https://developers.line.biz/ja/docs/messaging-api/)

## 📞 サポート

問題が発生した場合は、以下にお問い合わせください：

- GitHub Issues: https://github.com/TK7tk/yunicoroom/issues
- Manus サポート: https://help.manus.im

---

*最終更新: 2025-12-31*
